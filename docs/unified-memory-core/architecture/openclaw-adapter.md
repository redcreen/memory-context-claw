# OpenClaw Adapter Architecture

Purpose: consume Unified Memory Core outputs for OpenClaw retrieval and context assembly.

当前焦点：

- namespace mapping
- export consumption
- OpenClaw-specific projection
- plugin integration boundary
